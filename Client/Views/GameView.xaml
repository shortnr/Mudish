<Page x:Class="Client.Views.GameView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views"
      mc:Ignorable="d"  Background="AntiqueWhite"
      d:DesignHeight="600" d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="80"/>
        </Grid.ColumnDefinitions>

        <Border x:Name="GameListBorder" BorderThickness="3" BorderBrush="Black" Margin="8"
                Grid.ColumnSpan="2">
            <ListBox ItemsSource="{Binding GameLines}"
             BorderThickness="0"
             Background="Transparent"
             ScrollViewer.VerticalScrollBarVisibility="Auto"
             HorizontalContentAlignment="Stretch"
             Padding="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Padding="0 0 16 0"
                           Width="{Binding ActualWidth, ElementName=GameListBorder}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="IsHitTestVisible" Value="False"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <Border BorderThickness="3" BorderBrush="Black" Margin="8 4 4 8" Grid.Row="1" Grid.Column="0">
            <TextBox Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" Grid.Column="0"
                     Padding="2" VerticalAlignment="Center"/>
        </Border>

        <Button Content="Send" FontSize="18" Margin="4 4 8 8" IsDefault="True"
            Grid.Column="1" Grid.Row="1" Command="{Binding SendCommand}">
        </Button>
    </Grid>
</Page>
